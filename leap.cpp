#include<conio.h>
#include<stdio.h>
int main()
{
	int n,i;
	printf("enter the year:");
	scanf("%d",&n);
	if(i%4==0)
	printf("the given year is leap year");
	else
	printf("the given year is not leap year");
return 0;
}
